# Optional enhancement — NOT required for core pipeline
#
# mediapipe enables MediaPipe Hands geometry path inside the experimental
# hand_anomaly detector. Without it, hand_anomaly still runs using a
# heuristic ROI + edge-density fallback (lower quality; no benchmark GT).
#
# See README.md → Architecture → hand_anomaly
-r requirements.txt
mediapipe>=0.10
-e .
